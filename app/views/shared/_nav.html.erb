
<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to 'I Need Help!', root_path %></a></h1>
    </li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if logged_in? %>
        <li><a>Logged in as <%= current_user.name %></a></li>
        <li class="divider"></li>
        <li><%= link_to "Logout", login_path, :method => :delete %></li>
      <% else %>
        <li><%= link_to "Login", new_login_path %></li>
      <% end %>

      <% if logged_in? %>
        <li class="divider"></li>
	      <li class="has-form">
	        <%= link_to "New Problem", new_problem_path, :class => "button" %>
	      </li>
		  <% end %>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">
      <li class="divider"></li>
      <li><%= link_to 'All problems', problems_path %></li>
    </ul>
  </section>
</nav>