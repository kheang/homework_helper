<%= simple_form_for [@problem], remote: true do |form| %>
    My problem:
    <%= form.input :issue, :label => false %>
    What I tried:
    <%= form.input :try, :label => false %>

    <%= form.button :submit, 'Add Problem', class: "btn btn-large btn-success" %>

<% end %>
