<%= simple_form_for [problem], :html => { :multipart => true }, :remote => true do |form| %>
  My problem:
  <%= form.input :issue, :label => false %>
  What I tried:
  <%= form.input :try, :label => false %>
  Optional screenshot (valid filetypes: png, jpeg, gif)
  <%= form.file_field :screen %>
  <%= form.button :submit, 'Add Problem', class: "btn btn-large btn-success" %>
<% end %>
